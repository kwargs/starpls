name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Existing tag to release, for example v0.1.23"
        required: true
        type: string

jobs:
  build:
    name: Build
    runs-on: ${{ matrix.target.runs-on }}
    strategy:
      matrix:
        target:
          - name: linux-amd64
            runs-on: ubuntu-latest
          - name: linux-aarch64
            runs-on: ubuntu-latest
          - name: darwin-arm64
            runs-on: macos-14
      fail-fast: true

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.tag || github.ref }}
      - name: Prepare for build
        run: |
          bazel/setup-ci.sh
          mkdir -p editors/code/dist
        env:
          BUILDBUDDY_API_KEY: ${{ secrets.BUILDBUDDY_API_KEY }}
      - name: Build
        run: bazel build -c opt ${{ matrix.target.name == 'linux-aarch64' && '--platforms @zig_sdk//platform:linux_arm64' || '' }} //crates/starpls
      - uses: actions/upload-artifact@v4
        with:
          name: starpls-${{ matrix.target.name }}
          path: bazel-bin/crates/starpls/starpls

  build-windows:
    name: Build (Windows)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.tag || github.ref }}
      - name: Prepare for build
        run: |
          if [[ -n "${BUILDBUDDY_API_KEY:-}" ]]; then
            echo 'import %workspace%/bazel/remote-cache.bazelrc' >>.bazelrc
            echo "build --remote_header=x-buildbuddy-api-key=$BUILDBUDDY_API_KEY" >>.bazelrc
          fi
          echo "startup --output_user_root=C:/b" >>.bazelrc
          echo "startup --windows_enable_symlinks" >>.bazelrc
          mkdir -p editors/code/dist
        shell: bash
        env:
          BUILDBUDDY_API_KEY: ${{ secrets.BUILDBUDDY_API_KEY }}
      - name: Build
        run: bazel build -c opt //crates/starpls
      - uses: actions/upload-artifact@v4
        with:
          name: starpls-windows-amd64
          path: bazel-bin/crates/starpls/starpls.exe

  release:
    name: Release
    needs: [build, build-windows]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
          pattern: starpls-*
      - name: Prepare release artifacts
        run: |
          mkdir -p release

          install -m 0755 "$(find artifacts/starpls-linux-aarch64 -type f -name starpls | head -n 1)" release/starpls-linux-aarch64
          install -m 0755 "$(find artifacts/starpls-linux-amd64 -type f -name starpls | head -n 1)" release/starpls-linux-amd64
          install -m 0755 "$(find artifacts/starpls-darwin-arm64 -type f -name starpls | head -n 1)" release/starpls-darwin-arm64
          install -m 0755 "$(find artifacts/starpls-windows-amd64 -type f -name starpls.exe | head -n 1)" release/starpls-windows-amd64.exe
          chmod +x release/starpls-*

          # Also create archives for usage by installation methods like Homebrew.
          # Eventually, we'll remove the non-zipped binaries above.
          cd release
          cp starpls-linux-aarch64 starpls && tar -czvf starpls-linux-aarch64.tar.gz starpls && rm starpls
          cp starpls-linux-amd64 starpls && tar -czvf starpls-linux-amd64.tar.gz starpls && rm starpls
          cp starpls-darwin-arm64 starpls && tar -czvf starpls-darwin-arm64.tar.gz starpls && rm starpls
          cp starpls-windows-amd64.exe starpls.exe && zip starpls-windows-amd64.zip starpls.exe && rm starpls.exe
      - name: Show release artifacts
        run: find release
      - name: Create release
        run: |
          gh release create "$RELEASE_TAG" release/* --title "$RELEASE_TAG" --generate-notes --verify-tag
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RELEASE_TAG: ${{ github.event.inputs.tag || github.ref_name }}
