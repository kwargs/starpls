# Development dependencies for starpls.
#
# After `brew bundle`, run:
#   export PATH="/opt/homebrew/opt/rustup/bin:$PATH"
#   rustup toolchain install nightly-2023-12-06
#   cd editors/code && npx pnpm@8.6.7 install --frozen-lockfile

brew "bazelisk"
brew "buildifier"
brew "node"
brew "pnpm"
brew "protobuf"
brew "rustup"
